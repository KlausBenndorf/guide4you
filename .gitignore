# Emacs style *~ backup files
*~

# *.stackdump files occur if Perl fails
*.stackdump

Thumbs.db

# build/doc only contains generated files
/build/
/doc/

# CSS files in css/less only occur by accident
/less/*.css

# files in node_modules can always be obtained using npm
/node_modules

# brackets files are to be strictly local
.brackets.json*

# same holds for npm-debug.log files
npm-debug.log*

# exclude .orig files by kdiff3
*.orig

# exclude .idea folder of webstorm
.idea

# Ignore test logs
test.log
